# Temp notes for response fix
- Issue: `response.set_details({ _response })` breaks.
- Root cause: `value == {}` on numpy array.
- Fix: `if value is None or (isinstance(value, dict) and len(value) == 0)`.
- Test fix: "data" key is used in `build_return`.
- Healthcheck message is "Alive".
