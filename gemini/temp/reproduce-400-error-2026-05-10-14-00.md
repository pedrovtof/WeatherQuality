# Research Notes - 2026-05-10-14-00

- Found that `WeatherRequest` in `src/contracts/requests/weather_request.py` defines `hourly` as `Optional[List[str]]`.
- Logs show `hourly[]` being sent.
- Hypothesis: FastAPI returns 400 because it doesn't recognize `hourly[]` and maybe something else is missing or the validation is failing on the array format if it's not mapped correctly.
- Need to check `src/services/weather.py` to see how it uses the parameters.
- Also need to verify if `current[]` might have the same issue.

## To Do:
- [ ] Read `src/services/weather.py`
- [ ] Reproduce with `curl`
- [ ] Test adding `alias="hourly[]"` to `WeatherRequest`
